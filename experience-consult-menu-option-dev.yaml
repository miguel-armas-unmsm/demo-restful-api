springdoc:
  swagger-ui:
    path: /swagger/index.html
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

application:
  http-client:
    menu-options:
      base-url: ${BUSINESS_MENU_OPTION_BASE_PATH:http://localhost:8093/bbq/business/v1/}